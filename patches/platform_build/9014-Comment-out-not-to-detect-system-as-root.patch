From a0c9482c464e4563f9f6dbaa210bff2e8fbd3e73 Mon Sep 17 00:00:00 2001
From: AndroPlus <mail@andro.plus>
Date: Sat, 16 Feb 2019 14:11:12 +0900
Subject: [PATCH 1/1] Comment out not to detect system-as-root

Signed-off-by: AndroPlus <mail@andro.plus>
---
 core/Makefile | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

diff --git a/core/Makefile b/core/Makefile
index 10fc98479..d17996ffe 100644
--- a/core/Makefile
+++ b/core/Makefile
@@ -1240,9 +1240,9 @@ $(if $(BOARD_AVB_ENABLE),\
         $(hide) echo "avb_product_rollback_index_location=$(BOARD_AVB_PRODUCT_ROLLBACK_INDEX_LOCATION)" >> $(1)))
 $(if $(filter true,$(BOARD_USES_RECOVERY_AS_BOOT)),\
     $(hide) echo "recovery_as_boot=true" >> $(1))
-$(if $(filter true,$(BOARD_BUILD_SYSTEM_ROOT_IMAGE)),\
-    $(hide) echo "system_root_image=true" >> $(1)
-    $(hide) echo "root_dir=$(TARGET_ROOT_OUT)" >> $(1))
+#$(if $(filter true,$(BOARD_BUILD_SYSTEM_ROOT_IMAGE)),\
+#    $(hide) echo "system_root_image=true" >> $(1)
+#    $(hide) echo "root_dir=$(TARGET_ROOT_OUT)" >> $(1))
 $(if $(PRODUCT_USE_DYNAMIC_PARTITION_SIZE),$(hide) echo "use_dynamic_partition_size=true" >> $(1))
 $(if $(3),$(hide) $(foreach kv,$(3),echo "$(kv)" >> $(1);))
 endef
-- 
2.17.1

